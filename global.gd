extends Node

# Game Logistics variables
@export var training_mode = false
@export var break_the_targets = false
@export var start_flag = false
@export var pause_flag = false

# Gampleay variables
@export var AUTO_ATTACK_TIMEOUT = 1.5
@export var GOLD_TARGET_VAL = 3
@export var RED_TARGET_VAL = 1

# Called when the node enters the scene tree for the first time.
#func _ready():
	#pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
	#pass
